fun main() {
    /*
    Hard keywords
    The following tokens are always interpreted as keywords and cannot be used as identifiers:
    Here's a list of hard keywords in Kotlin:

* as
* as?
* break
* continue
* do
* else
* false
* for
* fun
* if
* in
* is
* interface
* internal
* null
* object
* package
* private
* protected
* public
* return
* super
* this
* throw
* true
* try
* typealias
* typeof
* val
* var
* when
* while



Soft keywords
The following tokens act as keywords in the context in which they are applicable, and they can be used as identifiers in other contexts:


    1. `by`
    2. `catch`
    3. `constructor`
    4. `delegate`
    5. `dynamic`
    6. `enum`
    7. `expect`
    8. `external`
    9. `finally`
    10. `get`
    11. `import`
    12. `infix`
    13. `init`
    14. `inline`
    15. `internal`
    16. `lateinit`
    17. `noinline`
    18. `open`
    19. `operator`
    20. `out`
    21. `override`
    22. `private`
    23. `protected`
    24. `public`
    25. `reified`
    26. `sealed`
    27. `set`
    28. `suspend`
    29. `vararg`
    30. `where`

   */
}